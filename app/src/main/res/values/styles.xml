<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme.White" parent="AppTheme.Base">
        <item name="colorPrimaryDark">@color/colorMaterialGray500</item>
    </style>

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyCurrentActionBar" parent="ThemeOverlay.AppCompat.Dark">
        <item name="android:tabStripEnabled">true</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <style name="MyCurrentActionBar.White" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:tabStripEnabled">true</item>
        <item name="android:textColorSecondary">@android:color/black</item>
        <item name="colorPrimaryDark">@color/colorMaterialGray500</item>
    </style>

    <style name="CustomTheme" parent="MaterialDrawerTheme">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="material_drawer_selected">@color/colorAccent</item>
        <item name="material_drawer_selected_text">@color/colorAccent</item>
    </style>
</resources>
